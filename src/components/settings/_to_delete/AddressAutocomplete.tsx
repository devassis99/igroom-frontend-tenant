/**
 * DEAD FILE — safe to delete, along with this _to_delete folder.
 *
 * Superseded by MapSearchField.tsx. Its contents were emptied rather than
 * removed because the tooling that made this change can't delete files on
 * your machine; the original imported `geocodeLocation`, which no longer
 * exists, so leaving the body in place broke `tsc`.
 */

